from django.contrib import admin
from main.models import Experience, Education

# Register your models here.
admin.site.register(Experience) # mendaftarkan model Experience
admin.site.register(Education) # mendaftarkan model Education